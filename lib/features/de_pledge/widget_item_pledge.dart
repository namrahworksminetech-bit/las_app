part of 'view_de_pledge.dart';

class ItemClient extends StatelessWidget {
  final DePledgeBloc bloc;
  final int index;

  const ItemClient(this.bloc, this.index, {super.key});

  // ClientModel get v => bloc.state.dataList[index];

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
